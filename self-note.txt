#self note 

REPL - Read, Eval, Print, Loop

Enter node on terminal and click TAB you can see the global variables that are available in node
For example I choose util. plus 2 click of tabs to see the other methods

2+5 = 7
_ + 3 = 10 // it shows that the result get from the underscore

fs = file system

`${__dirname}` = translates to the directory in which the script that we're currently executing is located.

JSON.parse = JSON.parse = take JSON Code, which is actually a string and will then automatically turn in JavaScript. A javascript object or array